services:
  app:
    build: .
    image: bim-webgpu-viewer
    container_name: bim_viewer_app
    ports:
      - "3000:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    restart: always
